```{=html}
<table class="quarto-listing-table table">
    <tbody class="list">
    <% for (const item of items) { %>
        <tr data-index="0" data-categories="null" <%= metadataAttrs(item) %>>
            <td>
                <% if ( item.audience == "Undergraduate Course" ) { %>
                    <span data-bs-toggle="tooltip" data-bs-placement="top" title="<%= item.audience %>"><i class="bi bi-stars"></i></span>
                <% } %>
                <% if ( item.audience == "Postgraduate Course" ) { %>
                    <span data-bs-toggle="tooltip" data-bs-placement="top" title="<%= item.audience %>"><i class="bi bi-bookmark-star-fill"></i></span>
                <% } %>
                <% if ( item.audience == "Reading Group" ) { %>
                    <span data-bs-toggle="tooltip" data-bs-placement="top" title="<%= item.audience %>"><i class="bi bi-people-fill"></i></span>
                <% } %>
                <% if ( item.audience == "Topics Course" ) { %>
                    <span data-bs-toggle="tooltip" data-bs-placement="top" title="<%= item.audience %>"><i class="bi bi-asterisk"></i></span>
                <% } %>
            </td>
            <td class="listing-url">
                <% if ( item.url ) { %>
                <span><a href="<%= item.url %>"><i class="bi bi-link"></i></a></span>            
                <% } %>
            </td>
            <td class="listing-coursecode">
                <%= item.coursecode %>
            </td>
            <td class="listing-title">
                <span data-bs-toggle="tooltip" data-bs-placement="top" title="<%= item.description %>"><%= item.title %></span>
            </td>
            <td>
                <% if ( item.semester == 3 ) { %>
                <span class="badge badge-primary" data-bs-toggle="tooltip" data-bs-placement="top" title="<%= item.when %>" style="background:darkorchid;"><%= item.year %></span>
                <% } %>
                <% if ( item.semester == 1 ) { %>
                <span class="badge badge-primary" data-bs-toggle="tooltip" data-bs-placement="top" title="<%= item.when %>" style="background:olivedrab;"><%= item.year %></span>
                <% } %>
                <% if ( item.semester == 2 ) { %>
                <span class="badge badge-primary" data-bs-toggle="tooltip" data-bs-placement="top" title="<%= item.when %>" style="background:orangered;"><%= item.year %></span>
                <% } %>
            </td>
            <td>
                <% if ( item.status == "Archive" ) { %>
                <i data-bs-toggle="tooltip" data-bs-placement="top" title="<%= item.status %>" class="bi bi-archive"></i>
                <% } %>
                <% if ( item.status == "Ongoing" ) { %>
                <i data-bs-toggle="tooltip" data-bs-placement="top" title="<%= item.status %>" class="bi bi-play-circle-fill"></i>
                <% } %>
                <% if ( item.status == "Upcoming" ) { %>
                <i data-bs-toggle="tooltip" data-bs-placement="top" title="<%= item.status %>" class="bi bi-calendar-check-fill"></i>
                <% } %>
            </td>
        </tr>
    <% } %>
</tbody>
</table>

```
