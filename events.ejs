```{=html}
<table class="quarto-listing-table table">
<tbody class="list">
<% for (const item of items) { %>
<tr data-index="0" data-categories="null" <%= metadataAttrs(item) %>>
    <td class="listing-url">
            <% if (item.url) { %>
            <a href="<%= item.url %>"><i class="bi bi-link-45deg"></i></a>
            <% } %>
        </td>
<td class="listing-title">
    <span ><%= item.title %></span>
</td>
<td class="listing-year">
    <span data-bs-toggle="tooltip" data-bs-placement="top" title="<%= item.duration %>"><%= item.year %></span></span>
</td>
<td class="listing-role">
    <span data-bs-toggle="tooltip" data-bs-placement="top" title="<%= item.role %>">
        <i class="bi bi-robot"></i>
    </span>    
</td>
<td class="listing-type">    
    <% if (item.type == "school") { %>
        <span data-bs-toggle="tooltip" data-bs-placement="top" title="School">
            <i class="bi bi-gem"></i>
        </span>
    <% } %>
    <% if (item.type == "workshop") { %>
        <span data-bs-toggle="tooltip" data-bs-placement="top" title="Workshop">
            <i class="bi bi-people-fill"></i>
        </span>
    <% } %>
    <% if (item.type == "event") { %>
        <span data-bs-toggle="tooltip" data-bs-placement="top" title="Event">
            <i class="bi bi-diamond-fill"></i>
        </span>
    <% } %>
</td>
</tr>
<% } %>
</tbody>
</table>

```

